<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap-integration.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         failOnRisky="true"
         failOnWarning="true">
    <testsuites>
        <testsuite name="Integration">
            <directory>./tests/Integration</directory>
        </testsuite>
    </testsuites>
    <groups>
        <include>
            <group>DB</group>
        </include>
    </groups>
    <!--
      No <source>/<coverage> block: coverage against a full-server run is driven
      from the CLI, and omitting it keeps failOnWarning green. Every integration
      class carries @group DB so it can also be run from the server's own phpunit.
    -->
</phpunit>
